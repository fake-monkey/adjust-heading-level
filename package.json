{
  "name": "structure-aware-markdown-headings",
  "displayName": "Structure-Aware Markdown Headings",
  "description": "Shift Markdown heading levels for document merging and splitting while preserving relative hierarchy.",
  "publisher": "false-ape",
  "icon":"./icon.png",
  "repository": {
    "url": "https://github.com/fake-monkey/adjust-heading-level.git"
  },
  "license": "Apache-2.0",
  "version": "0.2.3",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "vscode": "^1.62.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.increaseHeading",
        "title": "Increase Heading Level",
        "category": "Structure-Aware Markdown Headings"
      },
      {
        "command": "extension.decreaseHeading",
        "title": "Decrease Heading Level",
        "category": "Structure-Aware Markdown Headings"
      },
      {
        "command": "extension.batchAdjustHeading",
        "title": "Batch Adjust Heading Level",
        "category": "Structure-Aware Markdown Headings"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "package:vsix": "pnpm install && vsce package",
    "clean": "rimraf out",
    "compile": "pnpm run clean && tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.62.0",
    "typescript": "^4.5.2"
  }
}
